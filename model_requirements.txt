# Core dependencies for fine-tuned sentence transformer
sentence-transformers==5.1.2
torch==2.9.0
numpy==2.2.6
scikit-learn==1.7.2
scipy==1.15.3

# Optional: for development and training
# (not needed if just using the trained model)
